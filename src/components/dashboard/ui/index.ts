/**
 * Dashboard UI kit — dark-first primitives for the authenticated surface
 * (trainer + admin dashboards). Anything rendered under
 * `src/routes/_authenticated/` should reach for these before shadcn defaults,
 * which are tuned for the public/light marketing surface.
 *
 * See `mem://design/dashboard-ui-kit` for the locked token vocabulary.
 */
export * from "./dialog";
export * from "./button";
